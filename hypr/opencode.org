#+author: Nicola Ferru
#+title: opencode: cosa devi fare?

* Suddivisione dei file
in tanto la prima cosa da fare è suddividere il file hyprland.lua in:

- conf/window.lua (già esistente, ma da espandere)
  - va aggiunto il full screen per le finestre.
- conf/autorun.lua (già esistente, ma da espandere)
- conf/keybord-layout.lua (da creare, qui deve esserci tutto il necessario per la gestione del layout tastiera)
- conf/keybinding.lua (deve contenere tutti i keybinding ordinati)

* cosa bisogna verificare
- che tutti i file non presentino errori
- verificare che i .conf siano giusti
- verificare che tutte le app abbiano tutti i permessi, di presentazione in base allo standard di conf/permessi.lua.

